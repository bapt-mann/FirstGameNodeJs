{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",       // INDISPENSABLE pour Node.js
    "moduleResolution": "node",
    "outDir": "./dist",         // Le serveur compilé ira dans dist/
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    
    // On enlève "dom" pour éviter d'utiliser 'document' par erreur côté serveur
    "lib": ["es6", "DOM"]              
  },
  "include": ["src/**/*", "src/server.ts"], // Tout le serveur
  "exclude": ["dist/public/**/*"]            // On NE touche PAS au client
}